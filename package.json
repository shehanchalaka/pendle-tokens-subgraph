{
  "name": "pendle-tokens",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ shehanchalaka/pendle-tokens",
    "create-local": "graph create --node http://localhost:8020/ shehanchalaka/pendle-tokens",
    "remove-local": "graph remove --node http://localhost:8020/ shehanchalaka/pendle-tokens",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 shehanchalaka/pendle-tokens"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
